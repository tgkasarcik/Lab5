How to use:
    To compile and run project from TypeScript source code (found in ./src), do the following:
        -install Node.JS (if not already installed)
        -execute "npm run serve" in the root directory
        -project will open in web browser

Bouns attempted:
    None.

Browser and OS Support:
    Developed with Google Chrome on Windows 10